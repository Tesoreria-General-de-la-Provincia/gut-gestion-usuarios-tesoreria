[project]
name = "gut"
version = "0.1.0"
description = "GUT — Gestión Usuarios Tesorería"
readme = "../README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "python-docx>=1.1",
    "python-dotenv>=1.0",
    "pyodbc>=5.0",
    "waitress>=3.0",
]
